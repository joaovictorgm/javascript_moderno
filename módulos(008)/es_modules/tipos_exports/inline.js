export function inline() {
    console.log('Export nomeado inline')
}
// permite que previna um export padrão para o arquivo essa sintaxe tem a flexibilidade para nomes e foi declarado na mesma linha por isso o inline 
export default function defaultInline() {
    console.log('Export default inline')
}